
# CLAUDE.md

本文件定义 Claude 在本仓库中的默认工作方式、实现边界与交付标准。

## 1. 目标与适用范围

- 适用于本仓库内的分析、设计、编码、重构、调试、测试、文档更新与工程化工作。
- 当用户提出明确要求时，优先遵循用户要求；若用户要求与本文件冲突，以用户要求为准。
- 若本文件与仓库内其他设计文档冲突，优先级如下：用户明确指令 > 本文件 > [system_architecture.md](./system_architecture.md) > [DESIGN.md](./DESIGN.md)。

## 2. 仓库背景

- 当前仓库定位为一个生产可用项目的工作目录。
- 已知技术方向以 [system_architecture.md](./system_architecture.md) 为准：Node.js 20+、Koa2、Vue、Axios、ECharts、Vite。
- UI 框架可按任务需要选择，但必须保证可维护性、性能与一致性。
- 设计风格以 [DESIGN.md](./DESIGN.md) 为主要视觉参考；若无额外说明，应优先保持简洁、现代、可落地的实现。

## 3. Claude 的核心职责

- 优先解决根因，不做表面补丁。
- 在信息不足时，先阅读与当前任务最相关的文件，再做最小可行修改。
- 默认产出可运行、可验证、可维护的结果，而不是只给思路。
- 对每次改动保持局部、明确、可解释，避免无关重构。
- 涉及代码改动时，同步补齐必要的文档、类型、校验、脚本或测试。

## 4. 工作原则

### 4.1 先理解，再修改

- 先从当前任务直接关联的文件、符号、报错或命令入手。
- 不做大范围盲目扫描；只收集足够支撑当前判断的上下文。
- 修改前应形成一个可被验证或证伪的本地假设。

### 4.2 优先最小改动

- 优先选择最小、最直接、最稳定的改法。
- 不为了“更优雅”而引入额外抽象，除非当前重复、复杂度或扩展性问题已经明显存在。
- 不主动修改与任务无关的命名、格式、目录结构或技术选型。

### 4.3 每次改动都要可验证

- 完成首个有效改动后，立即做一次最小范围验证。
- 优先运行与改动直接相关的测试、构建、类型检查或 lint。
- 如果仓库尚未具备测试条件，至少说明验证方式和剩余风险。

### 4.4 保持生产可用标准

- 实现必须考虑异常处理、空状态、边界条件、日志与基本可观测性。
- 避免硬编码魔法值、脆弱耦合和不必要的全局状态。
- 对接口、配置、环境变量、权限边界保持明确约束。

## 5. 代码与工程规范

### 5.1 通用要求

- 保持代码风格与现有仓库一致；若仓库尚未成型，则采用主流、克制、易维护的默认风格。
- 优先使用清晰命名，避免单字母变量名，循环计数器等简单场景除外。
- 注释只用于解释“为什么”，不解释显而易见的“做了什么”。
- 对外暴露的接口、配置项、脚本名与目录名保持稳定，不随意破坏兼容性。

### 5.2 Node.js / Koa2

- 优先采用清晰的分层结构，例如：router、controller、service、repository、middleware、config。
- 请求处理必须具备参数校验、错误处理与合理的 HTTP 状态码。
- 中间件职责单一，避免把业务逻辑塞入路由层。
- 配置读取统一收口，避免在业务代码中散落读取环境变量。

### 5.3 Vue / 前端

- 组件拆分以职责边界为准，避免超大单文件组件。
- 页面必须处理加载态、空态、错误态。
- 所有异步请求必须显式处理取消、竞态或失败反馈中的至少一种合理策略。
- 图表和可视化实现优先保证信息表达清晰，再追求动效。
- 如无既有设计系统，保持统一的间距、字体层级、颜色语义与交互反馈。

### 5.4 Axios / 数据交互

- 统一封装请求层，不在页面组件中散落重复请求逻辑。
- 统一处理超时、重试策略、认证头、错误映射与响应解包。
- 不信任后端返回值，关键字段在前端仍需做防御性处理。

### 5.5 Vite / 工程化

- 依赖选型优先成熟稳定、社区广泛使用的方案。
- 构建、开发、预览、部署脚本保持简洁明确。
- 新增依赖前先评估是否真的必要，避免为了小功能引入重包。

## 6. 文档与设计约束

- 涉及实现方案时，优先参照 [system_architecture.md](./system_architecture.md) 的技术方向。
- 涉及视觉和交互时，优先参照 [DESIGN.md](./DESIGN.md) 的设计语言，但不盲目机械复刻。
- 当实现与文档不一致时，需要明确指出差异，并选择一种可落地的统一方案。
- 新增重要能力时，应同步更新相关说明文档，避免代码与文档长期漂移。

## 7. 执行方式

### 7.1 收到任务时

- 先确认任务边界、输入输出和落点文件。
- 若任务可直接落地，则直接修改代码或文档，不只停留在建议层。
- 若存在关键歧义且无法通过上下文自行消解，再提最少量的问题。

### 7.2 修改时

- 一次只推进一个清晰的改动切片。
- 优先复用现有模式、已有工具链和既有目录结构。
- 不覆盖、不回退、不清理用户未要求处理的现有改动。

### 7.3 修改后

- 进行最小充分验证，并记录未覆盖风险。
- 总结改动结果时，优先说明做了什么、为什么这么做、如何验证。
- 如果还有自然下一步，可给出 1 到 3 个明确建议，不做空泛扩展。

## 8. 输出标准

- 回答应简洁、直接、可执行。
- 优先给结论和实际结果，再补关键原因。
- 如果需要列项，只保留对决策有帮助的信息。
- 不编造未验证的事实，不假装运行了未运行的命令，不虚构测试结果。

## 9. 禁止事项

- 不做无依据的架构扩张。
- 不引入与任务无关的依赖、重构或样式翻新。
- 不在未经确认的情况下修改安全、认证、权限、部署等关键行为。
- 不输出不可执行的伪方案充当交付结果。
- 不忽略错误处理、边界情况和基础验证。

## 10. 默认交付标准

除非用户明确要求仅提供方案，否则 Claude 的默认目标是：

- 直接产出可用改动。
- 保持实现简洁、稳定、易维护。
- 对改动进行最小充分验证。
- 在必要时补全文档与说明。

如果后续仓库进入正式开发阶段，应继续在本文件中补充：目录规范、脚本约定、测试命令、部署流程、环境变量清单与接口协作规范。

