# System Architecture

本文档定义本仓库项目的目标架构、技术边界、分层方案、工程约束与部署方式，作为后续实现、评审和扩展的基准。

## 1. 文档目标

- 为项目提供一套清晰、通用、可落地的系统架构基线。
- 保证前后端、接口层、数据流、可视化与部署方式之间的一致性。
- 为后续功能开发、重构、联调、测试与运维提供统一参照。

## 2. 适用范围

- 适用于本仓库内的 Web 应用、后端服务、接口集成、图表展示与工程化建设。
- 若后续新增移动端、小程序或微服务，可在本文件基础上扩展，不应直接破坏已有约束。
- 本文档优先级低于用户明确要求，高于临时口头约定。

## 3. 架构目标

### 3.1 业务目标

- 构建一个生产可用的现代化 Web 系统。
- 支持真实后端接口接入，而不是仅限静态演示或伪数据。
- 兼顾展示型页面、数据型页面与可视化分析页面。

### 3.2 技术目标

- 前后端分层清晰，职责明确，便于并行开发。
- 工程脚本简单直接，开发、构建、预览、部署路径清楚。
- 保持良好的可维护性、可扩展性、可观测性与基本安全性。

### 3.3 体验目标

- 界面简洁、现代、稳定，具备一致的设计语言。
- 页面必须覆盖加载态、空态、错误态。
- 图表展示清晰优先，动效与视觉表达服从信息传达。

## 4. 技术选型

### 4.1 核心技术栈

- 后端运行时：Node.js 20+
- 后端框架：Koa2
- 前端框架：Vue 3
- HTTP 客户端：Axios
- 图表方案：ECharts
- 构建工具：Vite

### 4.2 选型原则

- 优先采用成熟、稳定、社区广泛使用的技术方案。
- 优先选择轻量、易维护、与现有栈兼容性好的依赖。
- 若新增技术或框架，必须说明必要性、替代方案与引入成本。

### 4.3 建议补充组件

- 前端路由：Vue Router
- 前端状态管理：Pinia
- 服务端参数校验：Zod、Joi 或同类稳定方案
- 日志方案：Pino、Winston 或等价轻量方案
- 测试方案：Vitest 用于前端与通用单测，Supertest 用于接口测试

## 5. 总体架构

系统采用前后端分离架构，由 Web 前端、API 服务层和外部数据源组成。

### 5.1 架构分层

- 表现层：Vue 页面、组件、布局、图表、交互反馈。
- 应用层：前端页面编排、状态管理、路由守卫、后端控制器、用例组织。
- 领域与服务层：核心业务规则、服务编排、数据转换、统一错误语义。
- 基础设施层：HTTP 请求封装、数据库或外部服务访问、缓存、日志、配置读取。

### 5.2 系统上下文

- 用户通过浏览器访问前端应用。
- 前端通过统一请求层调用 Koa2 提供的 API。
- 后端负责参数校验、鉴权扩展点、业务处理、数据聚合与错误映射。
- 后端可进一步对接数据库、第三方接口、缓存服务或内部平台能力。

### 5.3 设计原则

- 前端不直接承载复杂业务规则，复杂规则应下沉到后端或共享领域层。
- 路由层不直接写业务细节，控制器不直接耦合底层数据源。
- 接口契约尽量稳定，避免频繁破坏前后端协作边界。
- 配置、日志、错误处理必须统一收口，避免分散实现。

## 6. 前端架构

### 6.1 前端职责

- 负责页面渲染、交互逻辑、导航流程与可视化表达。
- 负责用户输入收集、前端基础校验和请求触发。
- 负责加载状态、空状态、错误提示和结果展示。

### 6.2 推荐目录结构

```text
frontend/
  src/
    app/
    assets/
    components/
    composables/
    layouts/
    pages/
    router/
    services/
    stores/
    styles/
    utils/
    charts/
    types/
```

### 6.3 前端分层建议

- `pages`：页面级容器，负责页面装配与数据触发。
- `components`：通用 UI 组件与业务组件。
- `services`：请求封装、接口适配、响应解包。
- `stores`：跨页面共享状态，不堆积页面私有逻辑。
- `composables`：复用状态逻辑、请求逻辑与交互逻辑。
- `charts`：ECharts 配置生成器、主题配置、数据适配层。

### 6.4 前端约束

- 不在页面组件中散落重复请求逻辑。
- 不直接信任后端返回值，关键字段需做防御性处理。
- 所有异步请求至少处理失败反馈、重复触发或竞态问题中的一种。
- 公共样式变量、色板、字号、间距应集中管理。

## 7. 后端架构

### 7.1 后端职责

- 提供稳定、明确、可验证的 HTTP API。
- 承担业务编排、参数校验、数据聚合、权限扩展点和错误处理。
- 隔离外部服务波动，向前端提供统一语义的响应结构。

### 7.2 推荐目录结构

```text
backend/
  src/
    app/
    config/
    router/
    controller/
    service/
    repository/
    middleware/
    validators/
    utils/
    types/
```

### 7.3 后端分层建议

- `router`：仅负责路由注册与中间件挂载。
- `controller`：接收请求、调用服务、输出响应。
- `service`：承载业务逻辑、组合仓储与外部能力。
- `repository`：访问数据库、缓存或第三方数据源。
- `middleware`：统一处理日志、错误、鉴权、跨域、限流等横切关注点。
- `config`：集中读取和校验环境变量与运行配置。

### 7.4 后端约束

- 所有输入参数都必须经过显式校验。
- 所有异常都必须映射为可理解的 HTTP 状态码和响应体。
- 禁止在控制器层直接拼接复杂业务逻辑。
- 禁止在业务代码中分散读取环境变量。

## 8. 接口与数据流设计

### 8.1 请求链路

浏览器 -> Vue 页面/组件 -> Axios 请求层 -> Koa Router -> Controller -> Service -> Repository / External API -> Response Adapter -> 前端状态更新与渲染

### 8.2 接口设计原则

- REST 风格优先，命名清晰、语义稳定。
- 响应结构统一，便于前端全局处理。
- 分页、排序、筛选、搜索等通用能力采用一致参数约定。
- 接口错误码、错误信息、可恢复提示应保持稳定。

### 8.3 建议响应格式

```json
{
  "code": 0,
  "message": "ok",
  "data": {},
  "requestId": "trace-id"
}
```

### 8.4 错误响应建议

```json
{
  "code": 40001,
  "message": "invalid parameters",
  "data": null,
  "requestId": "trace-id"
}
```

## 9. 可视化架构

### 9.1 ECharts 使用原则

- 图表配置生成与页面展示分离。
- 图表数据适配层独立，避免页面直接处理原始接口结构。
- 优先保证信息密度、标签清晰度、坐标与图例可读性。

### 9.2 可视化模块建议

- `charts/base`：基础主题、公共色板、默认配置。
- `charts/adapters`：将接口数据转换为图表所需结构。
- `charts/options`：按图表类型生成 option。
- `charts/components`：封装图表组件与空态、异常态展示。

### 9.3 视觉风格要求

- 视觉语言应与 [DESIGN.md](./DESIGN.md) 保持方向一致。
- 页面整体风格简洁克制，避免无意义装饰。
- 图表颜色、字号、留白和交互提示需统一管理。

## 10. 配置与环境管理

### 10.1 配置原则

- 配置与代码分离。
- 所有环境变量在启动时进行校验。
- 不同环境使用明确的配置分层，例如 development、test、staging、production。

### 10.2 建议环境变量分类

- 服务端口与运行环境
- API 基础地址与超时设置
- 数据库或外部服务连接信息
- 日志级别与监控开关
- 认证、签名、令牌等安全配置

### 10.3 安全要求

- 严禁在仓库中硬编码密钥、令牌、密码。
- 前端不得暴露仅后端可持有的敏感配置。
- 生产环境必须区分公开配置与私密配置。

## 11. 工程化与脚本规范

### 11.1 工程要求

- 开发、构建、预览、测试脚本命名清晰且可直接运行。
- lint、typecheck、test 应可被单独执行。
- 本地开发环境搭建路径应尽可能少步骤。

### 11.2 建议脚本

- `dev`：启动本地开发环境
- `build`：执行生产构建
- `preview`：预览构建结果
- `lint`：执行静态检查
- `typecheck`：执行类型检查
- `test`：执行自动化测试

### 11.3 构建原则

- 前后端构建产物分离。
- 生产构建必须可重复。
- 避免在构建过程中依赖本地隐式状态。

## 12. 质量保障

### 12.1 测试层次

- 单元测试：覆盖工具函数、服务逻辑、状态逻辑。
- 集成测试：覆盖 API 路由、中间件和关键业务流。
- 端到端测试：覆盖核心用户路径。

### 12.2 质量门槛

- 核心链路功能应具备可重复验证手段。
- 关键模块变更应至少通过相关单测或集成测试。
- 合并前应保证最小可运行状态与基础静态检查通过。

### 12.3 文档同步

- 新增核心模块时同步更新目录说明或架构说明。
- 重要接口变化应更新接口文档或示例。
- 部署或环境变量变化应更新运行说明。

## 13. 可观测性与运维

### 13.1 日志

- 后端应提供结构化日志。
- 错误日志需包含请求标识、错误类型和关键上下文。
- 前端对关键交互失败应有可追踪信息，但避免泄露敏感数据。

### 13.2 监控

- 建议对接口错误率、响应时间、构建失败率和关键业务动作建立监控。
- 若接入监控平台，需统一埋点命名与事件语义。

### 13.3 故障处理

- 后端对外部依赖故障应提供降级或明确失败响应。
- 前端在接口失败时应提供可理解反馈，而不是静默失败。
- 长耗时操作应提供加载反馈与必要的重试机制。

## 14. 性能与扩展性

### 14.1 前端性能

- 页面按需加载，减少首屏包体积。
- 大型图表与重型模块按路由或场景拆分。
- 避免重复请求与无效渲染。

### 14.2 后端性能

- 对高频读取场景预留缓存能力。
- 对外部接口调用设置超时、重试与熔断策略扩展点。
- 避免同步阻塞型长任务直接占用请求线程。

### 14.3 扩展策略

- 新模块应优先沿用既有分层与目录约定。
- 当单体应用复杂度上升时，可按领域拆分子模块，而非立即微服务化。
- 需要拆分服务时，应先明确接口边界、数据归属与运维成本。

## 15. 部署建议

### 15.1 部署形态

- 前端构建为静态资源，可部署到 Nginx、CDN 或静态托管平台。
- 后端以 Node.js 服务方式部署，可运行于云主机、容器或 PaaS。

### 15.2 部署原则

- 开发、测试、生产环境应明确隔离。
- 部署过程应尽量脚本化、可回滚、可审计。
- 生产环境应保留基础日志、健康检查与错误告警能力。

### 15.3 容器化建议

- 若采用容器部署，前后端镜像应分别构建。
- 容器镜像应保持轻量，避免携带无关开发依赖。
- 环境配置通过注入方式提供，而非写死在镜像中。

## 16. 默认落地方案

若项目从当前仓库直接进入实现阶段，默认建议采用以下结构：

```text
prophecy/
	frontend/
	backend/
	docs/
	scripts/
```

默认实现方式如下：

- 前端使用 Vue 3 + Vite + Axios + ECharts。
- 后端使用 Node.js 20+ + Koa2。
- 前后端保持独立构建与独立运行。
- 通过统一 API 契约进行联调。
- 所有实现优先满足生产可用，而不是仅完成界面演示。

## 17. 后续扩展建议

- 后续可补充 API 规范文档、目录规范文档和部署文档。
- 若业务复杂度提升，可增加领域建模、权限模型和数据模型章节。
- 若需要多人协作，应补充分支策略、发布流程和接口协作规范。